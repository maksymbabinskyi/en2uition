import { DataScrollDirective } from './data-scroll.directive';

describe('DataScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new DataScrollDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});
